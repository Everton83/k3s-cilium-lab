apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  name: first-pool
spec:
  blocks:
    - start: 192.168.3.201
      stop: 192.168.3.210
  disabled: false
# Nenhuma alocação ativa no momento: .201 era do clustermesh-apiserver
# (ClusterMesh foi desativado, ver values.yaml) e .202 era do MiniStack
# (movido pra fora do cluster, ver ../ministack/). O pool inteiro está livre
# pra qualquer Service do tipo LoadBalancer novo.
