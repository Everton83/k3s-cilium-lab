apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: l2-policy
spec:
  loadBalancerIPs: true
  # No nodeSelector / interfaces / serviceSelector => matches every LB service on
  # every node/interface. Fine for this single-node cluster.
